apiVersion: kubocd.kubotal.io/v1alpha1
kind: Release
metadata:
  name: spark-operator
  namespace: okdp-system
spec:
  package:
    repository: quay.io/kubotal/packages-dev/spark-operator
    tag: 0.3.0
  contexts:
    - name: default
      namespace: kubocd-system
  targetNamespace: spark-operator
  createNamespace: true
